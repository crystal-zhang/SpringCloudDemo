server:
  port: 7001

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/crystal-zhang/spring-cloud-config-server
          search-paths: config-repo
          username: crystal-zhang  #账号密码写真实的快一些我觉得，不使用也能访问有点慢
          password: zhanglei53121403